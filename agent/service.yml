apiVersion: v1
kind: Service
metadata:
  name: grafana-agent
  namespace: agent
spec:
  ports:
    - name: thrift-compact
      port: 6831
    - name: api
      port: 12345
  type: ClusterIP
  selector:
    name: grafana-agent