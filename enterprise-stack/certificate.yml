apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: grafana-tls
spec:
  dnsNames:
  - grafana.lab.home
  issuerRef:
    kind: ClusterIssuer
    name: mkcert
  secretName: grafana-tls
---
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: loki-tls
spec:
  dnsNames:
  - loki.lab.home
  issuerRef:
    kind: ClusterIssuer
    name: mkcert
  secretName: loki-tls
---
---
apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: prometheus-tls
spec:
  dnsNames:
  - prometheus.lab.home
  issuerRef:
    kind: ClusterIssuer
    name: mkcert
  secretName: prometheus-tls