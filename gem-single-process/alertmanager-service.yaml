apiVersion: v1
kind: Service
metadata:
  name: alertmanager
  namespace: gem
spec:
  ports:
    - name: gem-http
      port: 9000
      targetPort: http
  selector:
    name: alertmanager
  type: ClusterIP