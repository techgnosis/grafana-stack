apiVersion: v1
kind: Service
metadata:
  name: compactor-headless
  namespace: gem
spec:
  ports:
    - name: gem-http
      port: 9000
      targetPort: http
  selector:
    name: compactor
  type: ClusterIP
  clusterIP: None