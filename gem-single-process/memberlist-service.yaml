apiVersion: v1
kind: Service
metadata:
  name: gem-discovery
  namespace: gem
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: gem-gossip
      port: 7946
      targetPort: gossip
  selector:
    memberlist: "true"